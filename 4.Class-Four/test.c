#include <stdio.h>

int main()
{
      // logical OR- ||
      int minimumAge=20;
      
      int hrdioy=20,ajoy=19;

    //hrdioy  || ajoy
     // 1   ||  0 =true;
     // 0   ||  0 =false;
     //  0 ||  1 =true;
     // 1 ||  1 =true

//      printf("%d", (minimumAge<=hrdioy || minimumAge<=ajoy));
      //vowel A || E ||  I || O || U 

      // char ch;
      // printf("check  vowel: \n");
      // scanf("%c",&ch);  
      // printf("%d",(ch=='A' || ch=='E' || ch=='I' || ch=='O'|| ch=='U' || ch=='a' || ch=='e' || ch=='i' || ch=='o'|| ch=='u'));

      //logical NOT - ! 
      // 0-false;
      // 1-true
      
//      if(-0){
//       printf("test ...\n");
//      }

					// Assignment Operators

      
// Increment and Assignment ++ (post & pre fix)
// Decrement and Assignment –
// Add and assign +=
// Subtract and assign -=
// Multiply and assign *=
// Divide and assign /=
// Modulus and assign %=
//       Assignment =
      // int a = 50;

// Increment 
      // int a=10;
      // a=a-1;
      //    a++;  
      // printf("%d\n",a);

      //increment postFix
      // int x=10;
      //   printf("postfix : %d \n", x++);
      // printf("postfix after : %d\n", x);//11
           
      // printf("Prefix : %d\n", ++x);//12
      // printf("Prefix after : %d\n", x);//12

      //// Add and assign +=
      int a=5;
      // a=a+100;
      // a*=10;
      // a=a-10;
      // a=a*10;
      // a=a*5;
      // a*=5;

      printf("%d\n",a);

}